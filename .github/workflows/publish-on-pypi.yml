name: Publish on PyPI

on: [push, pull_request]

jobs:
  publish:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Set up Python 3.8
      uses: actions/setup-python@v1
      with:
        python-version: '3.8'

    - name: Install pypa/build
      run: >-
        python -m
        pip install
        build
        --user

    - name: Build a binary wheel and a source tarball
      run: >-
        python -m
        build
        --sdist
        --wheel
        --outdir dist/
        .

    - name: Publish distribution to PyPI
      uses: pypa/gh-action-pypi-publish@master
      with:
        user: __token__
        password: ${{ pypi-AgEIcHlwaS5vcmcCJDhmZDlhMjQ4LTE1YzItNDU4MS1hY2QwLTFkMTdkMjFkY2U1MwACJXsicGVybWlzc2lvbnMiOiAidXNlciIsICJ2ZXJzaW9uIjogMX0AAAYgz_cVije_jaBbzZU1q_9YQebrNj5z9xz_qIepcey73T0 }}
        verbose: true
